using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MuHua {
	/// <summary>
	/// 移动动作
	/// </summary>
	// public class KinesisMove {

	// 	public IKinesisMove move;
	// 	public CharacterKinesis kinesis;

	// 	public KinesisMove(Character character) {
	// 		if (character is StandardCharacter standard) {
	// 			StandardMove standardMove = new StandardMove(standard);
	// 			move = standardMove;
	// 			kinesis = standardMove;
	// 		}
	// 	}

	// 	/// <summary> 设置速度和加速度 </summary>
	// 	public void Speed(Vector2 moveDirection, float moveSpeed, float acceleration) {
	// 		move.Speed(moveDirection, moveSpeed, acceleration);
	// 	}
	// 	/// <summary> 初始化位置 </summary>
	// 	public void Initialize(Vector3 position, Vector3 eulerAngles) {
	// 		move.Initialize(position, eulerAngles);
	// 	}
	// }
	// public interface IKinesisMove {
	// 	/// <summary> 设置速度和加速度 </summary>
	// 	public void Speed(Vector2 moveDirection, float moveSpeed, float acceleration);
	// 	/// <summary> 初始化位置 </summary>
	// 	public void Initialize(Vector3 position, Vector3 eulerAngles);
	// }
}
