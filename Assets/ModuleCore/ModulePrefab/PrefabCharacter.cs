using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MuHua;

public class PrefabCharacter : MonoBehaviour {
	// public KinesisController controller;

	// public DataCharacter value;

	// public void UpdateVisual(DataCharacter value) {
	// 	this.value = value;
	// 	transform.position = value.position;

	// 	KinesisMove move = new KinesisMove(controller, value.moveInput);
	// 	controller.TransitionKinesis(move);
	// }

	// private void Update() {
	// 	if (value == null) { return; }
	// 	value.position = transform.position;
	// }
}
